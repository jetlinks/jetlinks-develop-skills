interface:
  display_name: "Agent Orchestration"
  short_description: "Orchestrate and verify bounded multi-agent work"
  default_prompt: "Use $agent-orchestration to select and execute the current route. Keep the primary as the only dispatch, integration, acceptance, and delivery owner; delegate bounded leaf slices with hard-disabled recursive tools where supported, collect Result Packets, and validate the coherent integrated stage."
